# Select applicable PStack principles

Read `principles/manifest.toml`. Select only principles whose trigger matches this change. Record each selected principle, decision, enforcement class, required artifact, and evidence in `pstack.principle-application.v1`. Do not load every skill into every worker.

This runtime asset is executed by the Gas City graph.
