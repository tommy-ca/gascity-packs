# Escalate only actionable blockers

Use Gas City graph state and the shared claim protocol. Do not invoke provider-native subagents. Produce the `pstack.program-status.v1` Markdown artifact at `pstack.artifact_path`. Include `goal`, `phase`, `predicate`, `blockers`, `restart_token`, and `evidence`; record only actionable blockers and preserve claim references and verification status.
