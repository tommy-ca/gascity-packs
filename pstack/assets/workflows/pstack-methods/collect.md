Read the supplied subject and selected skills. Capture only bounded evidence
for this formula's Cursor playbook identity in `pstack.playbook` metadata.

This runtime asset is executed by the Gas City graph. It does not run the
full Cursor playbook. It writes a bounded evidence artifact.
