## Why

The durable TRACEABILITY scenario "Live program is the recursive graph"
still says the plan names `pr-pstack-land-honesty` then
`pr-pstack-panel-stamp`. Remaining-units and the live program already
insert `pr-pstack-publish` between sling and panel stamp. Tests lock the
three ids in `docs/pstack-program-plan.md`. They do not lock that
scenario. A later restamp can keep the stale AND line while the plan
names publish.

`pstack/README.md` still says "Do not restamp it without a host sling"
without naming both formulas or saying gastownhall restamp is not the
publication vehicle. Appendix A of the program still talks as if catalog
restamp were a remaining dest.

## What Changes

- MODIFIED `pstack-delivery-evidence` requirement "PStack traceability
  references durable truth". The recursive-graph scenario names
  `pr-pstack-land-honesty` then `pr-pstack-publish` then
  `pr-pstack-panel-stamp`. Pack tests lock that scenario.
- `pstack/TRACEABILITY.md` and `pstack/README.md` match that graph.
- Program Appendix A stops treating catalog restamp as a remaining dest.
- This change does not sling, publish, restamp `registry.toml` hashes,
  stamp `gc.provider_panel`, or rename `pstack/pack.toml`.

## Capabilities

### New Capabilities

None.

### Modified Capabilities

- `pstack-delivery-evidence`: TRACEABILITY recursive-graph Gherkin names
  all three program ids.

## Impact

`openspec/specs/pstack-delivery-evidence/spec.md`,
`pstack/TRACEABILITY.md`, `pstack/README.md`,
`docs/pstack-program-plan.md`, and `pstack/tests/test_pstack_pack.py`.
Formulas stay unstamped. `registry.toml` is not restamped.
