## Why

Wait-on-scoped-name is closed. Dest still encodes queued submit and already
submitted first publication. Pytest presence-locks the second-publish
sentence. Staff land then recreates this leftover class. Dest is standing
policy only. Appendix C stays the event log.

## What Changes

- MODIFIED remaining-units. Hosted identity is `tommy-ca/pstack`. Drop
  dummy even-after identity. Drop queued submit. Drop Do not send a second
  publish request. Staff land is outside this checkout.
- MODIFIED first-pub. Drop already submitted. Keep one sling-not-go
  sentence that names `pstack-poteto-mode` and `pstack-build`.
- MODIFIED receipt. Drop queued submit. Drop Do not send a second publish
  request. Dry-run stays not submitted and not acceptance.
- Program leftover matches dest. Tests invert second-publish locks. Tests
  ban event-state from dest blocks.
- This leftover does not re-submit publish. It does not restamp
  gastownhall `registry.toml`.

## Capabilities

### New Capabilities

None.

### Modified Capabilities

- `pstack-delivery-evidence`: dest standing policy. Event log stays
  Appendix C.

## Impact

`openspec/specs/pstack-delivery-evidence/spec.md`,
`docs/pstack-program-plan.md`, `pstack/tests/test_pstack_pack.py`.
