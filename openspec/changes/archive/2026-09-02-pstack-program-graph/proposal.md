## Why

Four overlapping pstack plans still sequenced dest-env archive and `.audit/`
payloads. Isolation already lives in this repository. Operators need one
recursive graph.

## What Changes

- Name `docs/pstack-program-plan.md` as the live program.
- Keep durable Gherkin at `openspec/specs/`.
- Record that formula stamps wait on a Gas City compiler consumer, not
  another project checkout.

## Capabilities

### New Capabilities

None.

### Modified Capabilities

- `pstack-delivery-evidence`: TRACEABILITY names the live program plan.

## Impact

Operator docs and TRACEABILITY. Formulas stay unstamped.
