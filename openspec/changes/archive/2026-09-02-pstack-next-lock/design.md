## Context

Mapping-gaps is archived. Remaining program work is host sling, restamp, and
panel stamp. One pack-owned leftover is the dest-env grep standing in for the
restamped Gherkin AND. Arena judge is still a one-line gated producer.

## Goals / Non-Goals

**Goals:**

- Tests lock the live Durable Gherkin AND.
- Arena judge names the synthesis producer contract.

**Non-Goals:**

- No host sling.
- No registry restamp.
- No `gc.provider_panel` stamp.
- No distinct reviewer agents.
- No `pstack-figure-it-out` formula.

## Decisions

- Keep dest-env absence as an extra belt. Add the restamped AND and a
  `gherkin owner` ban on TRACEABILITY.
- Thicken `arena-judge.md` the way `interrogate-judgment.md` names schema,
  path, and validator. Formula TOML already gates the step.
- Do not record persona-split review or plan-review expansion.

## Risks / Trade-offs

- `gherkin owner` in TRACEABILITY would fail even as a denial sentence. That
  is intended. Ownership lives in the spec AND.

## Migration Plan

1. Author this change.
2. Add the failing test.
3. Thicken arena-judge.
4. Validate and archive.
5. Run pack tests.

## Open Questions

- None.
