## Why

`scripts/pstack_host_sling_proof.py` cooked and routed `pstack-poteto-mode`
(`fi-06k`) then `pstack-build` (`fi-awr`) in a disposable roles city. Both
`gc.routed_to` values were `fixture/gc.run-operator`. Remaining-units and
TRACEABILITY still say unproven. That is now false.

## What Changes

- MODIFIED remaining-units. Host sling is proven cook plus route. Next unit
  is hosted publish. Canonical-city sling stays forbidden. Do not restamp
  gastownhall `registry.toml` as publish. Do not stamp panel keys.
- MODIFIED TRACEABILITY durable-truth. Formula sling is proven cook plus
  route by `scripts/pstack_host_sling_proof.py`. Live-city import sentence
  stays.
- Pack tests lock the proven sentences and fail if unproven remains.
- This change does not publish, restamp hashes, or stamp panel keys.

## Capabilities

### New Capabilities

None.

### Modified Capabilities

- `pstack-delivery-evidence`: remaining-units and TRACEABILITY name proven
  cook-plus-route host sling.

## Impact

`openspec/specs/pstack-delivery-evidence/spec.md`,
`pstack/TRACEABILITY.md`, `pstack/REQUIREMENTS.md`,
`docs/pstack-program-plan.md`, `pstack/tests/test_pstack_pack.py`.
`registry.toml` is not restamped.
