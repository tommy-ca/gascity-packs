## Why

Interrogate of restamp-gate honesty. TRACEABILITY now forbids restamp even
after sling. Remaining-units program prose and spawn graph still say
without sling receipts. Tests freeze that allowing string. An owner can
restamp `29c84db` as catalog hygiene and cite spawn.

## What Changes

- MODIFIED remaining-units. Program MUST NOT restamp even after sling
  receipts.
- Spawn graph matches. Tests lock the closed sentence and fail if the
  allowing spawn line remains.
- This change does not restamp `registry.toml`. It does not publish.

## Capabilities

### New Capabilities

None.

### Modified Capabilities

- `pstack-delivery-evidence`: program restamp gate stays closed after sling.

## Impact

`openspec/specs/pstack-delivery-evidence/spec.md`,
`docs/pstack-program-plan.md`, `pstack/tests/test_pstack_pack.py`.
Then isolation HEAD fast-forwards tommy `main`.
