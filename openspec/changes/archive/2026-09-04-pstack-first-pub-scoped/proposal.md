## Why

Interrogate leftover after wait-on-scoped-name. First publication MUST
follow a host city that slings `pstack-poteto-mode` plus `pstack-build`.
That sling is proven. Same grammar as wait-on-receipts. Remaining-units
already waits on scoped-name. This requirement does not.

## What Changes

- MODIFIED First registry publication waits on host dogfood. Keep the
  title. Body says first publication waits on the scoped-name unit even
  after proven sling.
- Pack tests lock that sentence and fail if the old follow-a-host-city
  sling-only rule is the only gate.
- This change does not restamp, publish, or merge gastownhall.

## Capabilities

### New Capabilities

None.

### Modified Capabilities

- `pstack-delivery-evidence`: first publication waits on scoped-name.

## Impact

`openspec/specs/pstack-delivery-evidence/spec.md`,
`pstack/tests/test_pstack_pack.py`. Then FF tommy `main`.
