## Why

DESIGN still names `pstack.arena-runner`. Remaining-units already omits panel
keys, but dest standing does not freeze those sentences. Delivery evidence
does not fail closed if a formula grows those keys.

## What Changes

- MODIFIED remaining-units. Dest standing MUST needles cover the omit-panel
  sentences already in dest. Remaining-units MUST NOT contain
  `pstack.arena-runner`. Four identities stay distinct. Arena candidates stay
  `pstack.architect`. Interrogate review stays `pstack.reviewer`. Persona
  expansion is not N-model. Delivery evidence MUST fail closed if formulas
  grow those keys. `--root` MUST NOT relocate formula greps.
- MODIFIED methods. Keep MUST NOT stamp until consumer. Four identities.
  how, why, and architect stay sequential investigator. MUST NOT name
  `pstack.arena-runner`. Expansion is not N-model. Keep every scenario title.
  Fold mapping ANDs into Pack docs. Do not add a methods scenario title.
- DESIGN drops `pstack.arena-runner`. Panel children keep the step's existing
  run_target. Distinct run_target plus patch rows are rejected for pack
  formulas. TRACEABILITY names city `[[provider_panels]]` catalog ids. This
  checkout does not stamp. Keep the three-id live program.
- This leftover does not stamp panel keys. It does not restamp
  gastownhall `registry.toml`. It does not drop `pr-pstack-panel-stamp`.

## Capabilities

### New Capabilities

None.

### Modified Capabilities

- `pstack-delivery-evidence`: remaining-units panel mapping freeze.
- `pstack-gascity-pack`: methods panel mapping honesty.

## Impact

`openspec/specs/pstack-delivery-evidence/spec.md`,
`openspec/specs/pstack-gascity-pack/spec.md`,
`scripts/check_pstack_dest_standing.py`,
`scripts/check_pstack_delivery_evidence.py`, `pstack/tests/test_pstack_pack.py`,
`pstack/DESIGN.md`, `pstack/TRACEABILITY.md`.
