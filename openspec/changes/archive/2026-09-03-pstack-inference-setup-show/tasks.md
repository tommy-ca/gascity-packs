## 1. Specs

- [x] 1.1 Record ADDED `PStack setup formulas compile in the inference-gate
      city`. Do not MODIFY remaining-units. Do not MODIFY the write_city
      live-city requirement.

## 2. Honesty lock

- [x] 2.1 Lock the new requirement in `test_delivery_checks_cover_pstack`.
      Assert setup-only, `pstack-review`, `pstack-build`, and MUST NOT
      treat as sling. Keep the existing sling-unproven locks.
- [x] 2.2 Name the setup-only command in REQUIREMENTS Evidence Commands.
      Keep `Formula sling of pstack-poteto-mode and pstack-build remains
      unproven.`
- [x] 2.3 Name inference-gate setup-only show of `pstack-review` and
      `pstack-build` on the TRACEABILITY metadata evidence class only if
      the locked import and sling sentences stay.
- [x] 2.4 Record setup-only passed this session in Appendix A. Keep locked
      remaining-units strings. Pass host `check-plan.mjs`.

## 3. Apply

- [x] 3.1 Validate-only against this repository `openspec/`.
- [x] 3.2 Archive into this repository `openspec/`.
- [x] 3.3 Run `uv run --with pytest --with pyyaml pytest -q
      pstack/tests/test_pstack_pack.py`.
- [x] 3.4 Run `uv run --with pytest --with pyyaml env PYTHONPATH=. pytest
      -q tests/test_gascity_pack_inference_gate.py`.
- [x] 3.5 Run mapping-gaps validate-only. Run host `check-plan.mjs`.
