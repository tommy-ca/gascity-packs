## 1. Specs

- [x] 1.1 Record isolation on `feat/pstack-pack-honesty` and closed PR 385
      on remaining-units. Copy every live scenario. Add closed-PR and
      boot-recipe scenarios.

## 2. Honesty lock

- [x] 2.1 Update `docs/pstack-program-plan.md` intro, persist text, spawn
      graph, boot recipe, and Appendix A. Keep ids and the 97-box skeleton.
- [x] 2.2 Point REQUIREMENTS Evidence Commands at mapping-gaps without
      `--change`.
- [x] 2.3 Replace the two locked program sentences in
      `test_delivery_checks_cover_pstack`. Lock boot-recipe source from
      plan and REQUIREMENTS. Keep `change_name` on program-arm-list.
- [x] 2.4 One-line honesty on `docs/pstack-poteto-mode-router-plan.md`.
- [x] 2.5 One Delivery boundary sentence that isolation is on this branch
      and gastownhall PR 385 is closed unmerged.

## 3. Apply

- [x] 3.1 Validate-only against this repository `openspec/`.
- [x] 3.2 Archive into this repository `openspec/`.
- [x] 3.3 Run `uv run --with pytest --with pyyaml pytest -q pstack/tests/test_pstack_pack.py`.
