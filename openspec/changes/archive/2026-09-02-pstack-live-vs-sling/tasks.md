## 1. Specs

- [x] 1.1 Record the TRACEABILITY split as one added scenario on
      `PStack traceability references durable truth`.

## 2. Honesty lock

- [ ] 2.1 Replace `Live city sling remains unproven.` in
      `pstack/TRACEABILITY.md` with the two sentences named in apply-notes.
- [ ] 2.2 Lock both sentences in `test_delivery_checks_cover_pstack` and
      reject the old sentence.

## 3. Apply

- [ ] 3.1 Validate-only against this repository `openspec/`.
- [ ] 3.2 Archive into this repository `openspec/`.
- [ ] 3.3 Run `uv run --with pytest --with pyyaml pytest -q pstack/tests/test_pstack_pack.py`.
