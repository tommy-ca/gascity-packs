## Why

Setup-only compiled `pstack-review` and `pstack-build`. write_city showed all
40 formulas. Neither is sling. Remaining-units still names host sling of
`pstack-poteto-mode` then `pstack-build`. Arena picked that pair. Inference-gate
`pstack-review` then `pstack-build` launch is a different unit.

Cleanup drops a duplicated unproven sentence. It does not restamp
remaining-units.

## What Changes

- ADDED host sling receipts of `pstack-poteto-mode` then `pstack-build` as
  cook plus route in a disposable roles city.
- MODIFIED the inference-gate setup-only requirement. Drop the duplicated
  unproven AND. Keep show-is-not-sling.
- REQUIREMENTS Evidence Commands keep one Formula-sling unproven sentence.
- This change does not sling, publish, restamp hashes, stamp panel keys,
  or rename `pstack/pack.toml`.

## Capabilities

### New Capabilities

None.

### Modified Capabilities

- `pstack-delivery-evidence`: ADDED cook-plus-route receipt. Setup-only
  unproven AND removed.

## Impact

`openspec/specs/pstack-delivery-evidence/spec.md`,
`pstack/REQUIREMENTS.md`, `pstack/tests/test_pstack_pack.py`,
`docs/pstack-program-plan.md`. Formulas stay unstamped. `registry.toml`
is not restamped. This change does not sling.
