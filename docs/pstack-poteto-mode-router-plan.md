# PStack poteto-mode router note

The classify router landed on `feat/pstack-pack-honesty` as `46b584a`. It is on PR 385. This page is not a live program.

`pstack-poteto-mode` writes `pstack.route.v1` from `pstack/mappings/playbooks.toml`. It does not auto-sling. It does not set `gc.graph_operator`. Method skills `how`, `why`, `swarm`, `arena`, and `interrogate` stay off that map.

Production sequencing lives in `docs/pstack-production-readiness-plan.md`.
